var setting = {
    check: {
        enable: true,
        chkStyle: "checkbox"
    },
    data: {
        simpleData: {
            enable: true
        }
    }
};

function setCheck() {
    setting.check.radioType = 'all';
    $.fn.zTree.init($("#typeTree"), setting, zNodes);
}

$(function () {
    setCheck();
});
